# Pin to last stable Flet v0 release.
# 0.28.3 broke FilePicker on macOS; 0.80+ is a full API rewrite with known desktop bugs.
# 0.28.2 is the sweet spot: stable old API, FilePicker works, ElevatedButton etc. all work.
flet[all]==0.28.2

# Claude Agent SDK for real-time streaming
claude-agent-sdk>=0.1.0
anyio>=3.0.0

# PyInstaller for packaging desktop apps
pyinstaller>=6.0.0

# Suno automation
nodriver>=0.35        # stealth browser automation (no Selenium driver detection)
requests>=2.28.0      # direct HTTP calls to Suno's internal API
pyotp>=2.9.0          # TOTP 2FA code generation (optional, only if 2FA is enabled)
